{
  "name": "scm-price-history",
  "version": "0.0.3",
  "description": "Accessing the price history of an item",
  "main": "out/index.js",
  "scripts": {
    "build": "tsc",
    "example": "npm run build && node out/example.js",
    "start": "npm run build && node out/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "rm -rf out/ && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/HilliamT/scm-price-history.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/HilliamT/scm-price-history/issues"
  },
  "homepage": "https://github.com/HilliamT/scm-price-history#readme",
  "dependencies": {
    "axios": "^0.21.2",
    "cheerio": "^1.0.0-rc.3"
  },
  "devDependencies": {
    "typescript": "^4.0.2"
  }
}
